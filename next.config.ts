import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["example.com", "images.unsplash.com", "cdn.shutterstock.com", "picsum.photos", "instagram.fmkz1-2.fna.fbcdn.net"], // add your domain here
  },
  /* config options here */
};

export default nextConfig;
